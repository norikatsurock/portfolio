
<%= form_for(@album) do |f| %>
	<h4>タイトル</h4>
	<%= f.text_field :title %>
	<h4>時間</h4>
	<%= f.text_field :date %>
	<h4>場所</h4>
	<%= f.text_area :place %>

	

　　<%= f.submit '上書き' %>
<br>
<br>
<br>
	<%= link_to "写真の追加はこちら", admin_photos_path(@album), :style => 'color:grey;' %>


<% end %>